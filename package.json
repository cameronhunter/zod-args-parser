{
    "private": "true",
    "name": "@cameronhunter/mcp-cli-monorepo",
    "workspaces": [
        "packages/*/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "test": "vitest --config config/vitest/vitest.config.mts"
    },
    "devDependencies": {
        "@tsconfig/node-lts": "^22.0.1",
        "@tsconfig/strictest": "^2.0.5",
        "typescript": "^5.7.3",
        "vitest": "^3.0.5"
    }
}
